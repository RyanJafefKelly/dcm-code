# Prompt for GPT-5.5 Pro

I have pasted a context bundle for my current DCM work. Please use that bundle
as the basis for a rigorous Bayesian workflow plan.

Task: design a two-day synthetic-data validation / sensitivity analysis for the
Digital Consciousness Model, focused on whether root consciousness `C_s` and
internal binary feature/subfeature nodes are recoverable under realistic data
designs, and whether switching internal nodes to continuous probabilities has
low interpretational cost.

Please return:

1. A concise diagnosis of the statistical problem.
2. A recommended two-day plan, ordered by priority.
3. A table of estimands and diagnostics:
   - root `C_s`;
   - internal feature/subfeature states or probabilities;
   - tree transmission parameters;
   - path transmission slopes `delta_j`;
   - observation-layer parameters.
4. Clear guidance on which metrics are primary, secondary, or misleading.
5. A feasible simulation grid over sample size, rater-system overlap, root `C`
   values, model/DGP mismatch, exact-vs-composite tree, and binary-vs-three-state
   leaf.
6. Your view on simulation-based calibration:
   - whether to use it;
   - which quantities to run it on;
   - whether to use full GWT or a reduced tree;
   - what failure modes it would and would not diagnose.
7. Decision criteria for saying:
   - root `C_s` is adequately recoverable;
   - internal binary nodes are not meaningfully identifiable;
   - continuous internal nodes are defensible.
8. The strongest critique of this plan and framing.

Assume I can run a modest number of full GWT fits in two days, but not hundreds.
Keep the plan concrete enough that I can turn it into scripts immediately.
Write at peer level. Be sceptical and direct.

